# Environment Variables for Prompt Learning Cookbook
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Then edit .env with your actual API keys.

# Synth API key (get from https://app.usesynth.ai)
SYNTH_API_KEY=your-synth-api-key

# Environment API key (used to authenticate task app requests)
ENVIRONMENT_API_KEY=your-environment-key

# OpenAI API key (used by task app for LLM calls)
OPENAI_API_KEY=sk-your-openai-key

# Optional: Groq API key (alternative to OpenAI)
# GROQ_API_KEY=gsk-your-groq-key

# Optional: Override backend URL (for local development)
# BACKEND_BASE_URL=http://localhost:8000/api

# Optional: Task app URL (if not using default http://localhost:8001)
# TASK_APP_URL=https://your-tunnel-url.trycloudflare.com
