# RL Training Environment Variables
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   vim .env

# Required: Synth API key for training
SYNTH_API_KEY=your-synth-api-key

# Required: Environment key for task app authentication
ENVIRONMENT_API_KEY=your-environment-key

# Optional: OpenAI-compatible API key (for base model inference)
OPENAI_API_KEY=your-openai-key

# Optional: Task app port (default: 8114)
TASK_APP_PORT=8114

# Optional: Backend URL for local development
# SYNTH_BACKEND_URL=http://localhost:8000/api
